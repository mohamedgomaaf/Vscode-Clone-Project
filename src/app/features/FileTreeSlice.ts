import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  
};

const FileTreeSlice = createSlice({
  name: "fileTree",
  initialState,
  reducers: {},
});

// export const {} = FileTreeSlice;
export default FileTreeSlice.reducer;
